[pytest]
# Run async eval tests without needing an explicit marker on each one.
asyncio_mode = auto
# Put the project root on sys.path so `wealth_agent` and `tests` import cleanly.
pythonpath = .
testpaths = tests
